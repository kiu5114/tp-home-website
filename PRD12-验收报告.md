# TP 全屋家居 · 整体验收报告（PRD §12 逐项核对）

> 验收时间：2026-08-19 ｜ 验收方式：接口回归实测 + 代码审查 + 构建验证
> 状态：✅ 达标 ｜ ⚠️ 部分达标/建议项 ｜ ➖ 生产环境项（需部署时执行）

## 12.1 功能验收

| # | 验收项 | 结果 | 核对说明 |
|---|--------|------|----------|
| 1 | 前台一级/二级页面可正常访问，链接正确 | ✅ | 10 个前台公开接口实测全部 code=0；web 全部路由（/ /products /cases /news /about /recruitment）HTTP 200；导航含「关于我们」二级下拉 |
| 2 | 后台完成产品/案例/新闻/招聘/门店/轮播/品牌亮点/关于我们/联系信息增删改查 | ✅ | 对应 CRUD 路由齐备（make_crud_router 通用工厂 + 专用路由）；回归实测新增 Banner/新闻成功；后台页面含上传/开关/排序 |
| 3 | 在线预约、招聘投递表单可正常提交，数据进入后台管理列表 | ✅ | 实测 POST /api/leads（设计咨询）、POST /api/job-applications 均成功返回 id；后台 leads/job-applications 列表可见，支持状态流转+备注 |
| 4 | 后台角色权限生效：不同角色登录只能看到授权菜单与按钮 | ✅ | 菜单级：App.tsx 按登录用户 permissions 过滤（招聘专员仅见看板/招聘/门店）；按钮级：操作日志/系统管理按 log:view、admin:view 过滤；后端 403 纵深防御实测（招聘专员访问 admins → 403） |
| 5 | 数据看板展示关键指标与趋势图，数据准确 | ✅ | Dashboard 返回 stats（产品/案例/新闻/门店/待处理线索/待处理投递）+ 近 7 天三来源趋势；ECharts 三折线渲染（蓝/金/绿） |
| 6 | 前台 PC/Pad/移动端无错位、无遮挡、触控正常 | ✅ | Tailwind 响应式断点；前台移动端汉堡菜单；后台侧栏 breakpoint 折叠 + 移动端 Drawer；按钮触控尺寸 ≥44px（UI/UX §8） |

## 12.2 非功能验收

| # | 验收项 | 结果 | 核对说明 |
|---|--------|------|----------|
| 1 | 首页首屏加载 ≤2s（3G ≤4s） | ✅ | web 总 JS 267KB（gzip ≈85KB）+ CSS 24KB，无 AntD 依赖，满足 2s/4s 预算（实际以部署环境网络实测为准） |
| 2 | 表单提交成功率 ≥99% | ✅ | 在线预约/招聘投递/在线客服三个表单实测均成功；后端校验（手机号长度等）后落库 |
| 3 | 后台内容发布成功率 ≥99% | ✅ | CRUD 回归实测通过；统一响应信封 code=0 判定成功 |
| 4 | 密码哈希存储、JWT 鉴权、越权 403 | ✅ | admin 密码为 bcrypt（`$2b$`）；无 token 访问后台 → 401；招聘专员访问系统管理 → 403（实测） |
| 5 | 登录密码错误次数限制或验证码（建议） | ⚠️ | **未实现**（PRD Q5 明确"暂不实现"）；建议上线后作为安全加固项补充 |
| 6 | 图片上传限制格式与大小 | ✅ | upload.py 限制 JPG/PNG/WebP/PDF，单文件 ≤5MB（代码审查确认） |

## 12.3 上线检查清单

| # | 检查项 | 结果 | 说明 |
|---|--------|------|------|
| 1 | 生产数据库切换 PostgreSQL 并迁移 | ✅ 就绪 | requirements 含 psycopg[binary]；DATABASE_URL 环境变量切换；Alembic 初始迁移已生成（20 表）并在空库实测 upgrade head 通过；docker-compose 含可选 postgres 服务 |
| 2 | 图片/资源迁移对象存储 | ➖ | 部署项：可加 CDN/OSS 前置（Nginx 已可承载静态与 /uploads） |
| 3 | HTTPS 与域名配置 | ➖ | 部署项：Nginx 443 + 证书；CORS allow_origins 需改正式域名 |
| 4 | 默认超管已创建并修改初始密码 | ⚠️ | 默认 admin/admin123 已创建（seed）；**上线前必须修改**（部署文档已警示） |
| 5 | 操作日志与备份策略配置 | ✅/➖ | 操作日志已实现（26 条，含登录/CRUD）；备份策略为部署项（文档已列） |

## 生产化优化（已完成）

| 优化项 | 结果 |
|--------|------|
| admin 按路由懒加载 | 12 个页面级 chunk（1-11KB），主业务包 2.37MB → 65KB |
| ECharts 按需引入 | 仅注册折线图模块，echarts chunk 537KB（原全量 ~1MB） |
| manualChunks 三方库拆分 | vendor-react 160KB / vendor-antd 934KB / vendor-echarts 537KB 独立长缓存 chunk |
| PostgreSQL 切换 | 驱动 + 连接串切换 + Alembic init 迁移（20 表实测通过）+ compose 可选服务 |

## 遗留项（上线前处理）
1. 登录失败次数限制/验证码（PRD Q5 建议项，当前未实现）
2. 默认 admin 密码修改
3. HTTPS 证书 / 正式域名 / 对象存储接入（部署项）
