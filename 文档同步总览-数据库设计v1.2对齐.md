# 文档同步总览 — 对齐数据库设计文档 v1.2

> 事实源：数据库设计文档-TP全屋家居.md（v1.2，21 实体）
> 同步顺序：① 技术文档 → ② PRD（按用户要求）
> 完成日期：2026-08-18（变更记录落款 2026-08-19）

## 一、同步结果

| 文档 | 原版本 | 现版本 | 核心变更 |
|------|--------|--------|----------|
| 开发技术文档-TP全屋家居.md | v1.0 | **v1.1** | 22→21 实体；全表加 is_activate+审计列；admin/部门/产品/新闻重定义 |
| PRD-TP全屋家居企业官网与后台管理系统.md | v1.2 | **v1.3** | 同上；§9 加通用列约定；§15 变更记录补 v1.3 条目 |

> 注：PRD 变更记录已存在 v1.2 条目（案例字段调整），故本次数据模型对齐追加为 **v1.3**，未重复占用 v1.2。

## 二、关键对齐点（两文档一致）

1. **实体数 22→21**：移除 `ProductImage`，产品多图改用 `product.images`（JSON 串数组）。
2. **全表通用列**：`is_activate`(0禁用/1激活) + `created_at`(创建人/Admin FK) + `created_date`(创建时间) + `updated_at`(修改人) + `updated_date`(修改时间)。
3. **admin**：`real_name`→`name`；新增 `nickname`/`phone`/`email`；`username` 唯一；去 `status` 改用 `is_activate`。
4. **department**：新增 `parent_id`（自引用）。
5. **role**：`permissions` 改 TEXT/JSONB。
6. **product**：重定义为 `category_id`/`series_id`/`code`/`description`/`specs`(JSON)/`cover_image`/`images`(JSON)/`status`(0草稿/1上架/2下架)/`is_top`/`sort_order`；删除 `name`/`material`/`dimensions`/`view_count`/`is_recommended`/`space_category_id`。
7. **news**：重定义为 `source`(取代 author)/`is_published`(取代 status)/`is_top`/`expired_at`。
8. **推导实体 9→8**（移除 ProductImage）。

## 三、一致性校验结论（Task #70）

- ✅ 技术文档 `§4.3.` 恰好 21 节（.1–.21），ProductImage 段已删除并重排。
- ✅ 两份文档均无 `ProductImage` / 实际 `created_by` / `updated_by` 列名残留（"原 xxx" 仅为改名说明）。
- ✅ `is_activate` 覆盖：技术文档 34 处、PRD 23 处（21 表 + 说明）。
- ✅ PRD §9.1 = 21 实体；NewsArticle 字段正确。
- ✅ 技术文档 §12.1 已无 ProductImage 速查行；§12.3 已改 8 推导实体。

## 四、易错约定提醒

- `created_at` 语义 = **创建人(用户ID)**，`created_date` = 创建时间（与常见 `created_at`=时间 相反）。
- `is_activate` 与业务 `status` 共存，互不影响。
- 后续任何模型变更须三文档同步：数据库设计 → 技术文档 → PRD。
